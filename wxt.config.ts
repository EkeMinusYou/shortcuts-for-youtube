import { defineConfig } from "wxt";

export default defineConfig({
  srcDir: "src",
  manifestVersion: 3,
  manifest: {
    name: "Better Youtube Life",
    description: "Chrome extension to support additional shortcuts on YouTube.",
    version: "0.0.1",
  },
});
