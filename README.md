# shortcut-for-youtube

This is a chrome extension to support for additional shortcuts on Youtube.

## Develop
```shell
npm install
npm run dev   # Start dev server (wxt)
npm run build # Build production bundle (see wxt output path for the unpacked directory)
```

Load the build output shown by `wxt build` into Chrome as an unpacked extension.
